# restaurant_management

## 네이밍 컨벤션

- 파일/폴더명은 케밥케이스(kebab-case)로 작성한다. 예: `client/src/app.tsx`, `set-payment-mode.dto.ts`
- 예외: `README.md`, `LICENSE` 등 업계 관례상 대문자로 쓰는 파일은 그대로 둔다.
- 코드 내 식별자(컴포넌트명, 클래스명 등)는 각 언어/프레임워크 관례를 따른다 (예: React 컴포넌트는 `PascalCase` 식별자를 유지하되 파일명은 케밥케이스).
- `superpowers/plans/`, `superpowers/specs/`의 계획 문서를 작성할 때도 새로 만드는 파일 경로는 케밥케이스로 명시한다.
